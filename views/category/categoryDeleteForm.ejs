<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete <%= category.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-blue-100 min-h-screen">
    <%- include("../partials/header") %>
    <div class="max-w-xl mx-auto mt-10 bg-white rounded-lg shadow-lg p-8">
      <h1 class="text-3xl font-bold text-red-600 mb-6 text-center">
        Delete Category: <%= category.name %>
      </h1>
      <form
        action="/category/<%= category.id %>/delete"
        method="post"
        onsubmit="return confirm('Are you sure you want to delete this category? (All items belonging to that category will also be deleted!)');"
      >
        <%- include("../partials/category-form", { formAction:
        `/category/${category.id}/delete`, buttonText: "Delete", category,
        inputDisabled: true }) %>
      </form>
    </div>
  </body>
</html>
