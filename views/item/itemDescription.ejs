<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= item ? item.name : "Item details" %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-blue-100 min-h-screen">
    <%- include("../partials/header") %>
    <div class="max-w-xl mx-auto mt-10 bg-white rounded-lg shadow-lg p-8">
      <% if(item) { %> 
        <h1 class="text-3xl font-bold text-blue-700 mb-6 text-center">Item Details</h1>
        <div class="space-y-4">
          <div>
            <span class="font-semibold text-lg text-gray-800">Name:</span>
            <span class="font-bold text-blue-600"><%= item.name %></span>
          </div>
          <div>
            <span class="font-semibold text-lg text-gray-800">Quantity:</span>
            <span class="font-bold text-green-600"><%= item.quantity %></span>
          </div>
          <div>
            <span class="font-semibold text-lg text-gray-800">Category:</span>
            <span class="font-bold text-indigo-600"><%= item.category_name %></span>
          </div>
        </div>
        <div class="flex gap-4 mt-8 justify-center items-center">
          <a href="/item/<%= item.id %>/edit" class="bg-green-600 text-white px-6 py-2 rounded-md font-bold hover:bg-green-800 transition">Edit</a>
          <a href="/item/<%= item.id %>/delete" class="bg-red-600 text-white px-6 py-2 rounded-md font-bold hover:bg-red-800 transition">Delete</a>
          <a href="javascript:history.back()" class="text-blue-600 hover:underline font-semibold">Back to category (<%= item.category_name %>)</a>
        </div>
      <% } else { %>
        <h2 class="text-center text-red-600 font-semibold mt-8">There is no item like that! :(</h2>
        <div class="mt-8 text-center">
          <a href="javascript:history.back()" class="text-blue-600 hover:underline font-semibold">Go back</a>
        </div>
      <% } %>
    </div>
  </body>
</html>