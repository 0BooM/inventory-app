<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit: <%= item.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-blue-100 min-h-screen">
    <%- include("../partials/header") %>
    <div class="max-w-xl mx-auto mt-10 bg-white rounded-lg shadow-lg p-8">
      <h1 class="text-3xl font-bold text-red-600 mb-6 text-center">
        Delete Item: <%= item.name %>
      </h1>
      <form
        action="/item/<%= item.id %>/delete"
        method="post"
        onsubmit="return confirm('Are you sure you want to delete this item?');"
      >
        <%- include("../partials/item-form", { formAction:
        `/item/${item.id}/delete`, buttonText: "Delete item", item, categories,
        inputDisabled: true }) %>
      </form>
    </div>
  </body>
</html>
